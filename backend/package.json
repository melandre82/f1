{
  "name": "assignment-api-design",
  "version": "1.0.0",
  "description": "In this assignment, you will implement a web API following the theory of REST. We encourage you to have your ideas about the API service to build. Maybe you have some idea you want to start with through an API-driven design? For those of you without any ideas, we present a scenario below. That will also give a hint of the extent of this assignment.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "connection": "node src/scripts/test-connection.js",
    "populate": "node src/scripts/populate-db.js",
    "start": "node src/server.js",
    "devStart": "nodemon --inspect --max-old-space-size=4096 -r dotenv/config src/server.js",
    "lint": "npx eslint ./src || exit 0",
    "lint:fix": "npx eslint ./src --fix || exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.3",
    "morgan": "^1.10.0",
    "mysql2": "^3.9.7",
    "nodemon": "^3.1.0"
  },
  "devDependencies": {
    "@lnu/eslint-config": "^1.1.10",
    "eslint": "^8.57.0"
  },
  "eslintConfig": {
    "extends": "@lnu"
  }
}
